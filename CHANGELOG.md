# 更新日志

本项目按语义版本记录通用模板的功能、修复与迁移注意事项。版本号同时写入 `Packaging/Info.plist`。

## 0.1.4 — 2026-09-24

### 新增

- 输入仪表新增 EPM，与 APM 分行显示。
- 工作状态的顶部常驻小岛改为显示 EPM；没有最近操作时显示 `--`。

### 统计口径变更

- APM 改为传统自然时间口径：统计最近 60 秒内全部非自动重复按键和鼠标点击，不再按“活跃秒数”放大。
- EPM 使用同一个最近 60 秒窗口，但排除 Backspace 和 Forward Delete。
- Command、Control、Option 组合键及功能键会进入 APM/EPM；它们不会被误算进普通打字量。
- 滚轮和鼠标移动仍不计入 APM/EPM。

### 隐私与数据

- 输入统计仍只记录时间、类别和数量，不保存按键字符或实际输入内容。
- 每日记录格式保持兼容，原有工作、休息、喝水、AI、打字和设置数据无需迁移。

### 更新注意事项

- 0.1.4 的 APM 数值通常会低于旧版，因为停止操作的自然时间也包含在统计窗口内；这是预期变化。
- EPM 仅按本项目约定过滤删除键，不判断某次点击或快捷键是否在目标软件中真正产生效果。
- 覆盖旧 App 后如果输入统计不工作，请重新授权输入监控与辅助功能。

## 0.1.3 — 2026-09-24

### 修复

- 打包后的 App 统一从应用包资源目录加载图片，不再尝试访问仅供 SwiftPM 开发环境使用的资源包路径。
- 修复部分新版本 macOS 上通过安全检查后双击 App 仍立即退出、菜单栏图标完全不出现，并生成大量 `.ips` 崩溃报告的问题。

### 发布与兼容性

- 继续生成 Universal 2 应用，同时支持 Apple 芯片和 Intel Mac。
- 保留菜单栏面板的可靠打开逻辑、30 分钟自动待命、提醒、每日记录和输入统计等既有功能。
- 本版不改变记录文件格式，覆盖旧版本时不需要迁移本地设置或每日记录。

### 更新注意事项

- 分享时优先发送 `dist/角色效率岛.zip`，不要直接发送裸 `.app` 文件夹。
- 接收者首次打开仍可能需要右键 App 选择“打开”。
- 重新打包后，如果输入统计没有数据，应在“系统设置 → 隐私与安全性 → 输入监控/辅助功能”中移除旧条目，再重新添加新版 App。
